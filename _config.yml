title: Erik Schomburg
tagline: Data scientist & software engineer
description: >-
  Erik Schomburg — data scientist and software engineer.

url: "https://erikschomburg.com"
baseurl: ""

# Links rendered in the header/footer. Comment out or delete any you don't want
# public; add new ones and they'll show up automatically.
profiles:
  github: eschombu
  scholar: LRRVdOIAAAAJ
  # Uncomment to link LinkedIn. Left off for now because that profile names the
  # employers this site deliberately leaves generic.
  # linkedin: eschomburg

# Email is split into two halves and rejoined by JavaScript at render time, so
# the full address never appears in the served HTML. See _includes/email.html.
# Delete both lines to remove the email link entirely.
email_user: eschomburg
email_domain: gmail.com

markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge

exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - vendor
  - .venv
  - _private_assets
